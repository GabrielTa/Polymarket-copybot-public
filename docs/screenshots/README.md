# Screenshots

Drop PNGs here with these exact names so the main README renders them:

| Filename | What to capture |
|---|---|
| `main.png` | Main dashboard — KPI bar, open positions, signal feed |
| `shadow.png` | Shadow book — win rate of skipped bets by reason |
| `sizing.png` | Sizing experiment — flat vs conviction-weighted comparison |
| `timing.png` | Pre-game vs in-play — the timing discovery |

Tip: capture at a wide viewport (~1400px) in dark mode for the cleanest look.
